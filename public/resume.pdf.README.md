# public/resume.pdf

Drop your actual CV in this folder as `resume.pdf` (same directory as this note).
The Header "Resume" button and the home "Download CV" button both link to `/resume.pdf`.

If you prefer a different path, update `resumeUrl` in `lib/siteConfig.ts`.
